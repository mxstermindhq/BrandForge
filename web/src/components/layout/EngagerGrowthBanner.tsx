"use client";

/**
 * Growth banner removed as per user request.
 * Previously showed: "Grow·Paid tiers reserve listing placement, season visibility when enabled, 
 * and headroom for credits & AI limits as billing ships — see Plans."
 */
export function EngagerGrowthBanner() {
  return null;
}
