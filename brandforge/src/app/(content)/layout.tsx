/** Content pages — same global providers from root layout; no extra animation shell. */
export default function ContentLayout({
  children,
}: {
  children: React.ReactNode;
}): React.ReactNode {
  return children;
}
