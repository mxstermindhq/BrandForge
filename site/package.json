{
  "name": "brandforge-site",
  "private": true,
  "scripts": {
    "predeploy": "node scripts/inject-analytics.mjs",
    "deploy": "npm run predeploy && wrangler deploy",
    "preview": "wrangler dev",
    "assets": "node scripts/generate-favicons.mjs"
  }
}
